<h3>Add collaborator</h3>
<%= form_tag manage_collaborators_path, :method => "get" do %>
  <%= search_field_tag 'search', nil, placeholder: 'Enter query here', class: 'user-lookup' %>
  <%= submit_tag %>
<% end %> 

<% if @user_results.present? %>
  <div class="collaborators">
    <% @user_results.each do |user| %>
        <div class="collaborator-block">
          <ul>
            <li>User id: <%= user[:id] %></li>
            <li>Display name: <%= user[:display] %></li>
            <li>Affiliation: <%= user[:affiliation] %></li>
            <li>Email: <%= user[:email_address] %></li>
            <li>Verified professor: <%= user[:verified_professor] %></li>
          </ul>

          <%= form_tag manage_collaborators_path, method: "post", class: "collaborator-form" do %>
            <%= select_tag "role", options_for_select(['Owner', 'Editor', 'Reviewer']) %>
            <%= button_tag t('admin.action.manage_collaborators.add-collaborator'), class: 'collaborator-button', value: user[:id] %>
          <% end %> 
        </div>
      <% end %>
    </div> 
<% end %> 

<h3 class="change-collaborators">Change collaborators</h3>

<div class="collaborators">
  <% @object.collaborators.each do |collaborator| %>
    <% user = collaborator.user %>
    <div class="collaborator-block">
      <ul>
        <li>User id: <%= user.id %></li>
        <li><%= collaborator.role.capitalize %></li>
        <li>Display name: <%= user.display %></li>
        <li>Affiliation: <%= user.affiliation %></li>
        <li>Email: <%= user.email_address %></li>
        <li>Verified professor: <%= user.verified_professor %>
      </ul>
      <%= form_tag manage_collaborators_path, method: "delete", class: 'collaborator-form' do %>
        <%= button_tag t('admin.action.manage_collaborators.remove-collaborator'), class: 'collaborator-button', value: collaborator.id %>
      <% end %> 
    </div>  
  <% end %>
</div>


<!-- add popup when deleting user that says are you sure -->
<!-- post to search 
  < ajax autofill results> 

post to select user ( and create content collaborator w/ select roll )  -->