<section id="search"> 
	<% form_tag("/cases", :method => :get, :class => "search") do %>
	<ul>
		<li id="spinner">
			<img src="/images/ui/ajax-loader.gif" alt="" />
		</li>
		<% if current_user && false -%>
		<li class="btn">
			<a href="<%= new_case_path %>" class="btn-a new-action">
				<span>CREATE CASE</span>
			</a>
		</li>
		<% end -%>
		<li class="btn">
			<a href="#" class="btn-a btn-tags">
				<span>BROWSE TAGS</span>
			</a>
			<% form_tag(cases_path, :method => :get) do %>
			<div class="browse-tags-popup" id="cases_browse">
				<ul>
					<% @case_tags.each do |tag| %>
					<li>
						<span><%= check_box_tag("tags[]",tag.name, ((params[:tags] && params[:tags].include?(tag.name)) ? true : false), {:class => 'tag-control', :id => "tag-control-#{tag.id}"}) %>
						<strong><%= tag.name %></strong></span>
					</li>
					<% end -%>
				</ul>
				<div class="cl">&nbsp;</div>
				<div class="center">
					<input type="submit" class="button" value="Filter" />
					<%= link_to raw('Clear &raquo;'), cases_path, :class => 'clear' %>
				</div>
			</div>
			<% end -%>
		</li>
		<li class="field">
			<%= text_field_tag('keywords', params[:keywords], :size => 50) %>
		</li>
		<li class="submit">
			<input type="submit" class="submit" value="SEARCH" name="submit" />
		</li>
	</ul>
	<% end %>
</section>
<div class="cl"></div>
