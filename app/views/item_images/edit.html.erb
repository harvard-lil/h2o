<h1>Editing item_image</h1>

<% form_for(@item_image) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @item_image %> |
<%= link_to 'Back', item_images_path %>