<header class="users">
  <div class="content">
    <h1>
      <%= @user.display_name %>
      <% if @user.verified_professor? %>
        <div class="verified">
          <%= t 'users.profile.verified' %>
        </div>
      <% end %>
    </h1>
  </div>
</header>
<section class="profile">
  <div class="content"></div>
</section>
