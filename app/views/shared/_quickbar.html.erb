<% if @single_resource && [Case, TextBlock, Collage, Playlist].include?(@single_resource.class) && !@single_resource.new_record? -%>
<%= render :partial => "#{@single_resource.class.to_s.tableize}/quickbar" %>
<% end -%>
