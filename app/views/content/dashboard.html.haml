
%header.dashboard
  .content
    .content-inner
      = render 'content/search'

%section.dashboard
  .content
    %h2= t 'content.dashboard.headers.updates'
  .content
    %h2= t 'content.dashboard.headers.casebooks'
    = render partial: 'content/dashboard/content_browser', locals: {header: t('content.dashboard.headers.owned-casebooks'), content: @user.casebooks.owned, create_button: true}
    -# = render partial: 'content/dashboard/content_browser', locals: {header: t('content.dashboard.headers.saved-cases'), content: @user.casebooks.cases}
    = render partial: 'content/dashboard/content_browser', locals: {header: t('content.dashboard.headers.followed-casebooks'), content: @user.casebooks.followed}
  -# -# %script{type: 'text/template'}= render partial: 'content/dashboard/content_browser'
